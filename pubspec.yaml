name: persistencia_datos_local
description: Sistema de Persistencia de Datos Local
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  shared_preferences: ^2.2.2
  sqflite: ^2.3.0
  path: ^1.9.0
  crypto: ^3.0.3
  cupertino_icons: ^1.0.2
  http: ^1.1.2
  mailer: ^6.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/icono/tech.gif
